# Настройка многоязычного сайта Praxis Saewska

## 📋 Обзор

Сайт Praxis Saewska использует плагин `i18n_subsites` для создания полностью переведенных версий на 4 языках:
- 🇩🇪 **Немецкий** (`/de/`) - основной язык клиники
- 🇬🇧 **Английский** (`/en/`)
- 🇷🇺 **Русский** (`/ru/`)
- 🇺🇦 **Украинский** (`/uk/`)

## 🏗️ Архитектура

### Структура URL

Все языки имеют одинаковую структуру в отдельных подпапках.

**Важно:** Каждый язык имеет главную страницу по двум URL:
- `/{lang}/` → `/{lang}/index.html` (рекомендуемый, короткий URL)
- `/{lang}/index/` → `/{lang}/index/index.html` (альтернативный, из контента)

Это достигается настройкой `INDEX_SAVE_AS: 'index.html'` в `I18N_SUBSITES` для каждого языка.

```
https://praxis-saewska.de/
├── /de/                    # 🇩🇪 Немецкая версия
│   ├── /de/index/          # Главная страница
│   ├── /de/contact/        # Контакты
│   ├── /de/about/          # О нас
│   ├── /de/faq/            # FAQ
│   └── /de/privacy/        # Политика конфиденциальности
├── /en/                    # 🇬🇧 Английская версия
│   └── (та же структура)
├── /ru/                    # 🇷🇺 Русская версия
│   └── (та же структура)
└── /uk/                    # 🇺🇦 Украинская версия
    └── (та же структура)
```

### Автоматическое определение языка

Файл `.htaccess` автоматически определяет язык браузера пользователя и редиректит на соответствующую версию:

1. Пользователь заходит на `https://praxis-saewska.de/`
2. Сервер проверяет заголовок `Accept-Language` браузера
3. Редирект на соответствующий язык:
   - `Accept-Language: ru` → `/ru/`
   - `Accept-Language: uk` → `/uk/`
   - `Accept-Language: en` → `/en/`
   - По умолчанию → `/de/` (немецкий)

**Важно:** Используется 302 (временный) редирект, чтобы не влиять на SEO.

## ⚙️ Техническая конфигурация

### DEFAULT_LANG в Pelican

```python
DEFAULT_LANG = 'en'  # Технический язык
```

**Почему английский?**
- Плагин `i18n_subsites` НЕ загружает переводы для `DEFAULT_LANG`
- Английский используется как "технический" язык, где `msgid` (английский текст) = `msgstr` (перевод)
- Немецкий генерируется как полноценный подсайт `/de/` с переводами

### Отключение генерации корневого сайта

```python
INDEX_SAVE_AS = ''
AUTHORS_SAVE_AS = ''
CATEGORIES_SAVE_AS = ''
TAGS_SAVE_AS = ''
ARCHIVES_SAVE_AS = ''
```

Это отключает генерацию служебных страниц в корне, но страницы контента всё равно создаются для `DEFAULT_LANG`. Они не используются благодаря `.htaccess`.

## 🔨 Сборка сайта

### Автоматическая сборка

```bash
./build.sh
```

Скрипт автоматически:
1. ✅ Компилирует переводы (`.po` → `.mo`)
2. ✅ Генерирует все языковые версии
3. ✅ Выводит информацию о структуре

### Ручная сборка

```bash
# 1. Активировать виртуальное окружение
source .venv/bin/activate

# 2. Скомпилировать переводы
python3 compile_translations.py

# 3. Сгенерировать сайт
pelican content -s pelicanconf.py
```

## 📝 Добавление переводов

### 1. Обновление .po файлов

Переводы хранятся в `themes/med_praxis/translations/`:

```
themes/med_praxis/translations/
├── de/LC_MESSAGES/messages.po  # 🇩🇪 Немецкий
├── en/LC_MESSAGES/messages.po  # 🇬🇧 Английский
├── ru/LC_MESSAGES/messages.po  # 🇷🇺 Русский
└── uk/LC_MESSAGES/messages.po  # 🇺🇦 Украинский
```

Пример добавления перевода в `messages.po`:

```po
msgid "Book Appointment"
msgstr "Termin buchen"
```

### 2. Использование в шаблонах

В шаблонах Jinja2 используйте функцию `_()`:

```jinja2
<h1>{{ _('Book Appointment') }}</h1>
<p>{{ _('Welcome to our clinic') }}</p>
```

### 3. Компиляция

После изменения `.po` файлов:

```bash
python3 compile_translations.py
pelican content -s pelicanconf.py
```

## 🌐 Переключатель языков

В шаблоне `base.html` есть выпадающий список для смены языка:

```html
<select id="language-select">
    <option value="de">Deutsch</option>
    <option value="en">English</option>
    <option value="uk">Українська</option>
    <option value="ru">Русский</option>
</select>
```

JavaScript в `script.js` автоматически:
- Определяет текущий язык по URL
- Перенаправляет на соответствующую языковую версию при выборе

## 🧪 Тестирование локально

```bash
# Запустить локальный сервер
python -m http.server 8000 -d output

# Открыть в браузере
# Немецкий: http://localhost:8000/de/
# Английский: http://localhost:8000/en/
# Русский: http://localhost:8000/ru/
# Украинский: http://localhost:8000/uk/
```

**Примечание:** `.htaccess` работает только на Apache серверах. Для локального тестирования открывайте языковые подпапки напрямую.

## 📊 Проверка переводов

```bash
# Проверить переводы на всех языках
for lang in de en ru uk; do
    echo "=== $lang ==="
    grep -o 'section-title">[^<]*</h2>' output/$lang/contact/index.html | head -2
done
```

Ожидаемый результат:
- 🇩🇪 `/de/`: "Senden Sie uns eine Nachricht"
- 🇬🇧 `/en/`: "Send us a message"
- 🇷🇺 `/ru/`: "Отправьте нам сообщение"
- 🇺🇦 `/uk/`: "Надішліть нам повідомлення"

## ❗ Важные замечания

### SEO

1. **Используйте `hreflang` теги** в `<head>` для указания языковых версий (уже реализовано в `base.html`)
2. **302 редирект** не влияет на индексацию - каждый язык индексируется отдельно
3. **Sitemap** должен включать все языковые версии (уже настроено)

### Контент

1. Каждая страница должна существовать во **всех 4 языках**
2. Используйте одинаковые `slug` для связанных страниц:
   ```markdown
   # content/pages/contact.de.md
   Slug: contact
   Lang: de
   
   # content/pages/contact.en.md
   Slug: contact
   Lang: en
   ```

### Переводы шаблонов vs контента

- **Шаблоны** (навигация, кнопки, формы) → `.po` файлы
- **Контент** (текст страниц) → отдельные `.md` файлы для каждого языка

## 🔍 Устранение проблем

### Страница не открывается по адресу /{lang}/

**Проблема:** При переходе на `http://127.0.0.1:8000/uk/` сервер возвращает 404 или список файлов.

**Причина:** Отсутствует файл `/{lang}/index.html` в корне языковой папки. 

**Решение:** Добавьте `INDEX_SAVE_AS: 'index.html'` для каждого языка в `I18N_SUBSITES`:

```python
I18N_SUBSITES = {
    'uk': {
        'SITENAME': 'Praxis Saewska',
        'INDEX_SAVE_AS': 'index.html',  # ← Это исправляет проблему
        # ... другие настройки
    },
}
```

**Техническое объяснение:**
1. Глобальная настройка `INDEX_SAVE_AS = ''` отключает генерацию index.html для корневого сайта
2. Но она также влияет на подсайты, если не переопределена
3. Pelican генерирует страницу `index.md` как обычную страницу по правилу `PAGE_SAVE_AS = '{slug}/index.html'`
4. Результат: `/{lang}/index/index.html` создается, но `/{lang}/index.html` - нет
5. Переопределение `INDEX_SAVE_AS` в подсайте создает оба файла

### Переводы не отображаются

1. Проверьте, что `.po` файлы скомпилированы:
   ```bash
   ls themes/med_praxis/translations/de/LC_MESSAGES/messages.mo
   ```

2. Проверьте `DEFAULT_LANG = 'en'` в `pelicanconf.py`

3. Пересоберите сайт:
   ```bash
   ./build.sh
   ```

### Язык браузера не определяется

1. Проверьте, что `.htaccess` скопирован в `output/`
2. Убедитесь, что сервер поддерживает `.htaccess` (Apache)
3. Для других серверов (Nginx, Caddy) настройте редиректы в конфигурации сервера

### Неправильные ссылки

Проверьте, что в шаблонах используется `/{{ DEFAULT_LANG }}/` для навигации:

```jinja2
<a href="/{{ DEFAULT_LANG }}/">Home</a>
<a href="/{{ DEFAULT_LANG }}/contact/">Contact</a>
```

## 📚 Дополнительная информация

- [Документация i18n_subsites](https://github.com/getpelican/pelican-plugins/tree/master/i18n_subsites)
- [Pelican Themes](https://docs.getpelican.com/en/latest/themes.html)
- [GNU Gettext](https://www.gnu.org/software/gettext/manual/gettext.html)

---

**Последнее обновление:** 28 ноября 2024  
**Версия:** 2.0 - Полная поддержка многоязычности

