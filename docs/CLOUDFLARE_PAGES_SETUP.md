# Cloudflare Pages Deployment Setup

Этот документ описывает настройку деплоймента сайта на Cloudflare Pages с автоматическими языковыми редиректами.

## Что было сделано

1. ✅ Создан файл `_redirects` для серверных языковых редиректов
2. ✅ Обновлен `pelicanconf.py` для включения `_redirects` в статические файлы
3. ✅ Создан workflow `.github/workflows/cloudflare-pages.yml` для автоматического деплоймента
4. ✅ Обновлен `publishconf.py` с комментариями о настройке URL

## Преимущества Cloudflare Pages

- 🚀 **Серверные редиректы** - работают без JavaScript, как Apache `.htaccess`
- 🌍 **Глобальный CDN** - быстрая загрузка по всему миру
- 🔒 **Бесплатный SSL** - автоматические HTTPS сертификаты
- ♾️ **Неограниченный bandwidth** - без лимитов трафика
- 🔄 **Автоматический deployment** - при push в main
- 👀 **Preview deployments** - для Pull Requests

## Как работают редиректы

Файл `_redirects` содержит правила, которые Cloudflare Pages выполняет на сервере:

```
/ /ru/ 302 Language=ru    # Русскоязычные → /ru/
/ /uk/ 302 Language=uk    # Украинскоязычные → /uk/
/ /en/ 302 Language=en    # Англоязычные → /en/
/ /de/ 302 Language=de    # Немецкоязычные → /de/
/ /de/ 302                # По умолчанию → /de/
```

Cloudflare Pages анализирует HTTP заголовок `Accept-Language` и редиректит пользователей на соответствующую языковую версию.

## Настройка Cloudflare Pages

### Шаг 1: Получить API Token

1. Войдите на [dash.cloudflare.com](https://dash.cloudflare.com)
2. Перейдите в **My Profile → API Tokens**
3. Нажмите **Create Token**
4. Выберите шаблон **Edit Cloudflare Pages** или создайте кастомный с правами:
   - **Account.Cloudflare Pages** → Edit
5. Скопируйте созданный токен (он покажется только один раз!)

### Шаг 2: Получить Account ID

1. В Cloudflare Dashboard на главной странице
2. Найдите справа внизу **Account ID** (под Account Home)
3. Нажмите на иконку копирования
4. Скопируйте Account ID

### Шаг 3: Добавить секреты в GitHub

В вашем GitHub репозитории:

1. Перейдите в **Settings → Secrets and variables → Actions**
2. Нажмите **New repository secret**
3. Добавьте два секрета:
   - **Name:** `CLOUDFLARE_API_TOKEN`
     **Value:** ваш API токен из Шага 1
   - **Name:** `CLOUDFLARE_ACCOUNT_ID`
     **Value:** ваш Account ID из Шага 2

### Шаг 4: Создать проект в Cloudflare Pages (опционально)

Можно создать проект заранее или позволить GitHub Actions создать его автоматически:

#### Вариант A: Автоматическое создание
При первом запуске workflow GitHub Actions автоматически создаст проект `praxis-saewska` в Cloudflare Pages.

#### Вариант B: Ручное создание
1. В Cloudflare Dashboard → **Workers & Pages**
2. Нажмите **Create application** → **Pages** → **Connect to Git**
3. Подключите GitHub репозиторий
4. **Project name:** `praxis-saewska` (должно совпадать с `projectName` в workflow)
5. **Production branch:** `main`
6. **Build settings:**
   - **Framework preset:** None
   - **Build command:** оставьте пустым (сборка происходит в GitHub Actions)
   - **Build output directory:** `output`

### Шаг 5: Настроить кастомный домен (опционально)

Если у вас есть домен `praxis-saewska.de`:

1. В Cloudflare Pages проекте перейдите в **Custom domains**
2. Нажмите **Set up a custom domain**
3. Введите `praxis-saewska.de`
4. Cloudflare автоматически настроит DNS записи (если домен в Cloudflare)
5. Также добавьте `www.praxis-saewska.de` если нужно

## Первый деплоймент

После настройки секретов:

1. Закоммитьте и запушьте изменения:
   ```bash
   git add .
   git commit -m "Add Cloudflare Pages deployment with language redirects"
   git push origin main
   ```

2. Workflow автоматически запустится на push в `main`

3. Проверьте статус в **Actions** вкладке GitHub репозитория

4. После успешного деплоймента сайт будет доступен по адресу:
   - `https://praxis-saewska.pages.dev` (Cloudflare Pages домен)
   - `https://praxis-saewska.de` (если настроен кастомный домен)

## Ручной запуск деплоймента

Вы можете запустить деплоймент вручную через GitHub:

1. Перейдите в **Actions** → **Deploy to Cloudflare Pages**
2. Нажмите **Run workflow**
3. Выберите branch для деплоймента (по умолчанию `main`)
4. Нажмите **Run workflow**

## Тестирование языковых редиректов

После деплоймента протестируйте редиректы:

### Тест 1: Через curl
```bash
# Русский язык
curl -I -H "Accept-Language: ru" https://praxis-saewska.pages.dev/
# Должен вернуть: Location: /ru/

# Английский язык
curl -I -H "Accept-Language: en" https://praxis-saewska.pages.dev/
# Должен вернуть: Location: /en/

# Без заголовка (по умолчанию немецкий)
curl -I https://praxis-saewska.pages.dev/
# Должен вернуть: Location: /de/
```

### Тест 2: Через браузер
1. Откройте DevTools (F12)
2. Network tab → Preserve log
3. Откройте `https://praxis-saewska.pages.dev/`
4. Проверьте редирект на нужный язык

### Тест 3: Изменение языка браузера
1. Chrome/Edge: Settings → Languages → Add language → Move to top
2. Firefox: Settings → Language → Choose → Add
3. Откройте сайт в инкогнито для проверки нового языка

## Обновление SITEURL для продакшена

В `publishconf.py` обновите SITEURL если используете кастомный домен:

```python
# Для кастомного домена:
SITEURL = 'https://praxis-saewska.de'

# Или для Cloudflare Pages домена:
# SITEURL = 'https://praxis-saewska.pages.dev'
```

## Мониторинг и логи

- **Cloudflare Dashboard:** Workers & Pages → praxis-saewska → View build logs
- **GitHub Actions:** Repository → Actions → последний workflow run
- **Analytics:** Cloudflare Dashboard → Web Analytics

## Troubleshooting

### Проблема: Workflow не запускается
- ✓ Проверьте, что секреты добавлены в GitHub
- ✓ Убедитесь что push идет в ветку `main`
- ✓ Проверьте права Actions в Settings → Actions → General

### Проблема: Build failed
- ✓ Проверьте логи в GitHub Actions
- ✓ Убедитесь что `uv sync` успешно выполняется
- ✓ Проверьте что `compile_translations.py` не выдает ошибок

### Проблема: Редиректы не работают
- ✓ Проверьте что файл `_redirects` существует в `output/`
- ✓ Проверьте синтаксис в `_redirects` файле
- ✓ Подождите несколько минут после деплоймента (CDN кэш)

### Проблема: 404 страницы
- ✓ Убедитесь что все языковые версии правильно сгенерированы
- ✓ Проверьте структуру папок в `output/` директории
- ✓ Проверьте логи сборки Pelican

## Сравнение с GitHub Pages

| Функция | GitHub Pages | Cloudflare Pages |
|---------|-------------|------------------|
| Серверные редиректы | ❌ Нет | ✅ Да (_redirects) |
| Языковые редиректы | ❌ Только JS | ✅ Серверные |
| CDN | ✓ GitHub CDN | ✓ Cloudflare CDN |
| SSL | ✓ Бесплатный | ✓ Бесплатный |
| Bandwidth | 100GB/месяц | ♾️ Неограничен |
| Build minutes | 2000/месяц | ♾️ Неограничены |
| Preview deploys | ❌ Нет | ✅ Да (для PR) |
| Analytics | Базовые | Расширенные |

## Дополнительные возможности

### Preview Deployments для Pull Requests
Добавьте в workflow:
```yaml
on:
  pull_request:
    branches:
      - main
```

### Cloudflare Web Analytics
1. Workers & Pages → praxis-saewska → Settings → Web Analytics
2. Включите **Enable Web Analytics**
3. Скопируйте beacon ID
4. Добавьте скрипт в шаблон сайта

### Custom Headers
Создайте файл `_headers` для кастомных HTTP заголовков:
```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 1; mode=block
  Referrer-Policy: strict-origin-when-cross-origin
```

## Полезные ссылки

- [Cloudflare Pages Documentation](https://developers.cloudflare.com/pages/)
- [Redirects Documentation](https://developers.cloudflare.com/pages/configuration/redirects/)
- [GitHub Actions для Cloudflare Pages](https://github.com/cloudflare/pages-action)
- [Custom Domains Setup](https://developers.cloudflare.com/pages/configuration/custom-domains/)

