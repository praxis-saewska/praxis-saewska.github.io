# Лучшие практики i18n_subsites для Pelican

## 📖 Введение

Этот документ описывает проверенные практики настройки многоязычных сайтов на Pelican с использованием плагина `i18n_subsites`.

## 🎯 Ключевые принципы

### 1. DEFAULT_LANG не получает переводы автоматически

**Проблема:**
```python
DEFAULT_LANG = 'de'  # Немецкий как основной
I18N_SUBSITES = {'en': {...}, 'ru': {...}}  # Английский и русский
```

Результат: Немецкий сайт генерируется БЕЗ переводов (отображаются msgid).

**Причина:** 
Плагин `i18n_subsites` инициализирует gettext только для языков из словаря `I18N_SUBSITES`, но не для `DEFAULT_LANG`.

**Решение 1 (Рекомендуемое):** Технический DEFAULT_LANG + полное отключение корня

```python
DEFAULT_LANG = 'en'  # Английский как технический язык

# Полностью отключаем генерацию DEFAULT_LANG в корне
INDEX_SAVE_AS = ''
PAGE_SAVE_AS = ''        # ← Важно! Отключает контент-страницы
ARTICLE_SAVE_AS = ''     # ← Важно! Отключает статьи
AUTHORS_SAVE_AS = ''
CATEGORIES_SAVE_AS = ''
TAGS_SAVE_AS = ''
ARCHIVES_SAVE_AS = ''

I18N_SUBSITES = {
    'de': {  # Немецкий как полноценный подсайт
        'SITENAME': 'My Site',
        'INDEX_SAVE_AS': 'index.html',
        'PAGE_SAVE_AS': '{slug}/index.html',    # ← Включаем для подсайта
        'ARTICLE_SAVE_AS': '{slug}/index.html', # ← Включаем для подсайта
        # ... переводы работают ✅
    },
    'en': {  # Английский тоже как подсайт
        'SITENAME': 'My Site',
        'INDEX_SAVE_AS': 'index.html',
        'PAGE_SAVE_AS': '{slug}/index.html',
        'ARTICLE_SAVE_AS': '{slug}/index.html',
        # ... переводы работают ✅
    },
    'ru': {  # Русский как подсайт
        'SITENAME': 'My Site',
        'INDEX_SAVE_AS': 'index.html',
        'PAGE_SAVE_AS': '{slug}/index.html',
        'ARTICLE_SAVE_AS': '{slug}/index.html',
        # ... переводы работают ✅
    },
}
```

**Результат:** В `output/` будут ТОЛЬКО папки `/de/`, `/en/`, `/ru/` без корневых страниц.

**Почему английский?**
- Если переводы не загружаются, отображаются msgid (английский текст)
- Для английского msgid = msgstr, поэтому отсутствие переводов незаметно
- Все реальные языки (включая немецкий) получают полные переводы

### 2. Правильная структура URL

**Проблема:** `/{lang}/` не открывается (404)

**Причина:** Отсутствует `/{lang}/index.html`

**Решение:** Настройка `INDEX_SAVE_AS` для каждого подсайта

```python
I18N_SUBSITES = {
    'de': {
        'INDEX_SAVE_AS': 'index.html',  # ← ОБЯЗАТЕЛЬНО
        # Создаст /de/index.html
    },
}
```

**Без этой настройки:**
- ❌ `/de/` → 404 (нет index.html)
- ✅ `/de/index/` → работает (страница из content/pages/index.de.md)

**С этой настройкой:**
- ✅ `/de/` → работает (INDEX)
- ✅ `/de/index/` → работает (PAGE)

### 3. Пустой SITEURL для относительных путей

**Рекомендация:**
```python
I18N_SUBSITES = {
    'de': {
        'SITEURL': '',  # Пустой для правильных путей
    },
}
```

**Почему:**
- CSS/JS/изображения используют абсолютные пути от корня: `/theme/css/styles.css`
- Если `SITEURL = '/de'`, путь становится `/de/./theme/css/styles.css` (неправильно)
- С пустым `SITEURL` путь остается `/./theme/css/styles.css` (работает)

### 4. Навигация между языками

**Проблема:** Ссылки ведут на корень `/` вместо `/{lang}/`

**Решение:** Использовать переменную для языкового префикса

```python
# В pelicanconf.py для каждого языка:
I18N_SUBSITES = {
    'de': {
        'LANG_PREFIX': '/de',  # Для использования в шаблонах
    },
}
```

```jinja2
<!-- В шаблоне base.html -->
<a href="{{ LANG_PREFIX|default(SITEURL, true) }}/">Home</a>
<a href="{{ LANG_PREFIX|default(SITEURL, true) }}/contact/">Contact</a>
```

**Или лучше:** Использовать `DEFAULT_LANG` напрямую

```jinja2
<a href="/{{ DEFAULT_LANG }}/">Home</a>
<a href="/{{ DEFAULT_LANG }}/contact/">Contact</a>
```

### 5. Автоопределение языка браузера

**Лучшая практика:** .htaccess с 302 редиректом

```apache
RewriteEngine On

# Определить язык браузера
RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^ru [NC]
RewriteRule ^$ /ru/ [R=302,L]

RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^de [NC]
RewriteRule ^$ /de/ [R=302,L]

# По умолчанию
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /de/ [R=302,L]
```

**Важно:** Используйте 302 (временный), не 301 (постоянный)
- 302 не кэшируется браузерами
- Позволяет пользователю менять язык
- Не влияет на SEO (каждый язык индексируется отдельно)

### 6. SEO: hreflang теги

**Обязательно добавьте в `<head>`:**

```jinja2
<!-- Текущий язык -->
<link rel="alternate" hreflang="{{ DEFAULT_LANG }}" 
      href="{{ SITEURL }}/{{ DEFAULT_LANG }}{{ output_file|replace('/index.html', '/') }}" />

<!-- Альтернативные языки -->
{% for lang, settings in I18N_SUBSITES.items() %}
<link rel="alternate" hreflang="{{ lang }}" 
      href="/{{ lang }}{{ output_file|replace('/index.html', '/') }}" />
{% endfor %}

<!-- x-default для неопределенного языка -->
<link rel="alternate" hreflang="x-default" href="/de/" />
```

### 7. Компиляция переводов

**Всегда компилируйте .po → .mo перед генерацией:**

```bash
#!/bin/bash
# В build.sh

# 1. Компилировать переводы
python3 compile_translations.py

# 2. Генерировать сайт
pelican content -s pelicanconf.py
```

**Без компиляции:** переводы не работают (gettext не найдет .mo файлы)

### 8. Структура контента

**Правильная организация файлов:**

```
content/pages/
├── index.de.md      # Главная - немецкий
├── index.en.md      # Главная - английский
├── index.ru.md      # Главная - русский
├── contact.de.md    # Контакты - немецкий
├── contact.en.md    # Контакты - английский
├── contact.ru.md    # Контакты - русский
```

**Метаданные каждого файла:**
```markdown
Title: Kontakt
Lang: de
Slug: contact
```

**Важно:**
- `Lang:` определяет язык страницы
- `Slug:` должен быть одинаковым для переводов одной страницы
- Pelican автоматически связывает страницы с одним slug

## 📋 Чек-лист настройки

- [ ] `DEFAULT_LANG = 'en'` (технический язык)
- [ ] Все реальные языки в `I18N_SUBSITES`
- [ ] **Глобально отключить корень:**
  - [ ] `INDEX_SAVE_AS = ''`
  - [ ] `PAGE_SAVE_AS = ''` ⚠️ Важно!
  - [ ] `ARTICLE_SAVE_AS = ''` ⚠️ Важно!
  - [ ] `AUTHORS_SAVE_AS = ''`
  - [ ] `CATEGORIES_SAVE_AS = ''`
  - [ ] `TAGS_SAVE_AS = ''`
  - [ ] `ARCHIVES_SAVE_AS = ''`
- [ ] **Для каждого языка в `I18N_SUBSITES`:**
  - [ ] `INDEX_SAVE_AS = 'index.html'`
  - [ ] `PAGE_SAVE_AS = '{slug}/index.html'`
  - [ ] `ARTICLE_SAVE_AS = '{slug}/index.html'`
  - [ ] `SITEURL = ''` (пустой)
- [ ] `.po` файлы для всех языков
- [ ] Скрипт компиляции переводов
- [ ] `.htaccess` для автоопределения языка
- [ ] `hreflang` теги в `<head>`
- [ ] Переключатель языков в навигации
- [ ] Тестирование всех языков
- [ ] **Проверка:** в `output/` НЕТ корневых папок (about/, contact/ и т.д.)

## 🐛 Типичные ошибки

### 1. "Переводы не работают для DEFAULT_LANG"

```python
# ❌ НЕПРАВИЛЬНО
DEFAULT_LANG = 'de'
I18N_SUBSITES = {'en': {...}, 'ru': {...}}

# ✅ ПРАВИЛЬНО
DEFAULT_LANG = 'en'
I18N_SUBSITES = {'de': {...}, 'en': {...}, 'ru': {...}}
```

### 2. "404 на /{lang}/"

```python
# ❌ НЕПРАВИЛЬНО - INDEX_SAVE_AS не указан
I18N_SUBSITES = {
    'de': {'SITENAME': 'Site'},
}

# ✅ ПРАВИЛЬНО
I18N_SUBSITES = {
    'de': {
        'SITENAME': 'Site',
        'INDEX_SAVE_AS': 'index.html',
    },
}
```

### 3. "CSS не загружается"

```python
# ❌ НЕПРАВИЛЬНО - SITEURL добавляет префикс
I18N_SUBSITES = {
    'de': {'SITEURL': '/de'},
}
# Результат: /de/./theme/css/styles.css ❌

# ✅ ПРАВИЛЬНО
I18N_SUBSITES = {
    'de': {'SITEURL': ''},
}
# Результат: /./theme/css/styles.css ✅
```

### 4. "Переводы не компилируются"

```bash
# ❌ НЕПРАВИЛЬНО - только генерация
pelican content -s pelicanconf.py

# ✅ ПРАВИЛЬНО - сначала компиляция
python3 compile_translations.py
pelican content -s pelicanconf.py
```

### 5. "В корне output/ есть папки about/, contact/, faq/"

```python
# ❌ НЕПРАВИЛЬНО - PAGE_SAVE_AS не отключен
INDEX_SAVE_AS = ''
# Но PAGE_SAVE_AS = '{slug}/index.html' всё ещё активен!

# ✅ ПРАВИЛЬНО - отключить ВСЁ для корня
INDEX_SAVE_AS = ''
PAGE_SAVE_AS = ''       # ← Обязательно!
ARTICLE_SAVE_AS = ''    # ← Обязательно!

# И включить для подсайтов
I18N_SUBSITES = {
    'de': {
        'PAGE_SAVE_AS': '{slug}/index.html',
        'ARTICLE_SAVE_AS': '{slug}/index.html',
    },
}
```

**Причина:** `INDEX_SAVE_AS` отключает только служебные страницы (индекс, архивы, категории), но не контент-страницы. Для полного отключения корня нужно также установить `PAGE_SAVE_AS = ''` и `ARTICLE_SAVE_AS = ''`.

## 📚 Ресурсы

- [i18n_subsites документация](https://github.com/getpelican/pelican-plugins/tree/master/i18n_subsites)
- [Pelican документация](https://docs.getpelican.com/)
- [GNU Gettext мануал](https://www.gnu.org/software/gettext/manual/)
- [Google hreflang руководство](https://developers.google.com/search/docs/specialty/international/localized-versions)

---

**Проверено на:** Pelican 4.9.1, i18n_subsites 1.0.0  
**Дата:** 28 ноября 2024

