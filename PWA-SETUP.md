# PWA Setup Guide для Praxis Saewska

## ✅ Что уже сделано

1. ✅ Создан `manifest.json` с метаданными приложения
2. ✅ Создан `service-worker.js` для кэширования и офлайн-работы
3. ✅ Добавлена регистрация Service Worker в `script.js`
4. ✅ Добавлены PWA мета-теги во все HTML файлы
5. ✅ Добавлена ссылка на manifest во все HTML файлы

## 📱 Что нужно сделать

### 1. Создать иконки для PWA

Иконки нужны для отображения приложения на главном экране устройства.

#### Вариант A: Использовать генератор иконок

1. Откройте файл `generate-icons.html` в браузере
2. Нажмите "Generate All Icons" или "Download All Icons"
3. Сохраните все иконки в корневую директорию сайта

#### Вариант B: Создать иконки вручную

Создайте PNG иконки следующих размеров в папке `images/`:
- `images/icon-72x72.png` (72x72 пикселей)
- `images/icon-96x96.png` (96x96 пикселей)
- `images/icon-128x128.png` (128x128 пикселей)
- `images/icon-144x144.png` (144x144 пикселей)
- `images/icon-152x152.png` (152x152 пикселей)
- `images/icon-192x192.png` (192x192 пикселей)
- `images/icon-384x384.png` (384x384 пикселей)
- `images/icon-512x512.png` (512x512 пикселей)

**Рекомендации:**
- Используйте цвет темы: `#00C9A7` (teal)
- Фон: белый или прозрачный
- Можно использовать логотип из `images/file.svg` как основу
- Иконки должны быть квадратными
- Важные элементы должны быть видны даже на маленьких размерах

#### Вариант C: Онлайн-генераторы

Можно использовать онлайн-сервисы:
- https://realfavicongenerator.net/
- https://www.pwabuilder.com/imageGenerator
- https://favicon.io/

### 2. Обновить Service Worker при изменении контента

При обновлении контента сайта нужно обновить версию в `service-worker.js`:

```javascript
const CACHE_NAME = 'praxis-saewska-v1.0.1'; // Увеличьте версию
```

Это заставит браузеры загрузить новую версию кэша.

### 3. Тестирование PWA

#### В Chrome DevTools:

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Application"
3. Проверьте:
   - **Manifest** - должен загружаться без ошибок
   - **Service Workers** - должен быть зарегистрирован и активен
   - **Cache Storage** - должны быть созданы кэши

#### Тест установки:

1. В Chrome на мобильном устройстве:
   - Откройте сайт
   - В меню браузера выберите "Добавить на главный экран"
   - Или нажмите на баннер "Установить приложение" (если появится)

2. В Chrome на десктопе:
   - В адресной строке появится иконка установки
   - Или через меню: "Установить Praxis Saewska..."

#### Тест офлайн-режима:

1. В DevTools → Network → выберите "Offline"
2. Обновите страницу
3. Сайт должен работать из кэша

### 4. Проверка через Lighthouse

1. Откройте DevTools → Lighthouse
2. Выберите "Progressive Web App"
3. Запустите проверку
4. Исправьте найденные проблемы

## 🔧 Дополнительные настройки (опционально)

### Добавить уведомление об обновлении

В `script.js` уже есть базовая логика обновления. Можно добавить UI-уведомление:

```javascript
// В service worker registration
if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
    // Показать уведомление пользователю
    if (confirm('Доступна новая версия сайта. Обновить?')) {
        newWorker.postMessage({ type: 'SKIP_WAITING' });
    }
}
```

### Настроить стратегию кэширования

В `service-worker.js` используется стратегия "Cache First". Можно изменить на:
- **Network First** - для часто обновляемого контента
- **Stale While Revalidate** - для баланса между скоростью и актуальностью

## 📋 Чеклист перед публикацией

- [ ] Все иконки созданы и загружены
- [ ] Manifest.json проверен (можно проверить через https://manifest-validator.appspot.com/)
- [ ] Service Worker работает (проверено в DevTools)
- [ ] Сайт работает офлайн
- [ ] PWA можно установить на устройство
- [ ] Lighthouse PWA score > 90
- [ ] HTTPS включен (обязательно для PWA)

## 🚀 После публикации

1. Проверьте, что сайт доступен по HTTPS
2. Протестируйте установку на реальных устройствах
3. Проверьте работу офлайн-режима
4. Мониторьте ошибки в консоли браузера

## 📚 Полезные ссылки

- [MDN: Progressive Web Apps](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)
- [Web.dev: PWA](https://web.dev/progressive-web-apps/)
- [PWA Builder](https://www.pwabuilder.com/)

## ⚠️ Важные замечания

1. **HTTPS обязателен** - Service Workers работают только через HTTPS (или localhost)
2. **Иконки обязательны** - без иконок PWA не будет работать корректно
3. **Обновление кэша** - не забывайте обновлять версию кэша при изменении контента
4. **iOS ограничения** - на iOS PWA работает, но с ограничениями (push-уведомления только с iOS 16.4+)

